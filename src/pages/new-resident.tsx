import { AppLayout } from '@/components/layout/app-layout';
import { NewResidentForm } from '@/components/residents/new-resident-form';

const NewResidentPage = () => {
  return (
    <AppLayout>
      <NewResidentForm />
    </AppLayout>
  );
};

export default NewResidentPage;